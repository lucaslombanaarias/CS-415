# CS 415

Coursework for CS 415 (Game Development) at UIUC; MP1 is an Unreal Engine 5.6 tunnel runner in `MP1/InfiniteMatrixStarter`.
Unreal assets are stored with Git LFS, so run `git lfs pull` after cloning.
